systemLog:
   destination: file
   path: /app/mongodb/log/mongo.log  
   logAppend: true
storage:
   dbPath: /app/mongodb/data
   journal:
      enabled: true
processManagement:
   fork: true
net:
   bindIp: 0.0.0.0
   port: 27017
setParameter:
   enableLocalhostAuthBypass: false